# KatoBizScraper
Simple Python Program to Scrape the Mankato CoC and export to a CSV File.

## Requirements
You'll need to have the requests and BeautifulSoup packages installed. You can quickly do this by running:

	python -m pip install requests BeautifulSoup4

## Usage
Simply run the following line to start the program.

	python main.py
